class NodeGroup {
    val selfId = NodeId()
    var memberMap = emptyMap<NodeId, GroupMember>()

    init {
        var endpoint = NodeEndpoint

        object endpoint {
            endpoint.getId()
        }
    }

    constructor()
}